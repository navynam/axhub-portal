# AX-HUB 디자인 가이드 — 신한라이프 CI 기준

> 기준 CI: [신한라이프 CI 소개](https://www.shinhanlife.co.kr/hp/cdhh0230.do) · [신한 브랜드 CI](https://www.shinhanci.co.kr/01_company/about06_01.jsp)
> 브랜드 컬러 원칙: **“세련되고 확신에 찬 신뢰감·규모의 블루”** + **“발전·열정의 골드”**
> 모든 토큰은 `src/styles.css` 의 `:root` CSS 변수로 정의되어 전 화면에 일관 적용됩니다.

---

## 1. 브랜드 컬러

### 주색 — 신한 블루 (Shinhan Blue)
| 역할 | 토큰 | HEX | 비고 |
|------|------|-----|------|
| 주 브랜드·액션 | `--navy` | `#0046FF` | Shinhan Blue · Pantone 2388C |
| 보조(Royal) | `--navy-lt` | `#2878F5` | 그라데이션·호버 |
| 최심(Navy) | `--navy-deep` | `#00236E` | 히어로·FAB 그라데이션 시작 |
| 옅은 틴트 | `--navy-soft` | `#E8EEFF` | 활성 메뉴 배경·칩 |

용도: 로고 마크, 1차 버튼(`.btn-primary`), 사이드바 활성, 링크/강조, 채팅 사용자 말풍선, 플로팅 버튼, 탭 언더라인.

### 보조색 — 신한 골드 (Shinhan Gold)
| 역할 | 토큰 | HEX |
|------|------|-----|
| 강조 | `--accent` | `#C6982E` |
| 골드 위 텍스트 | (고정) | `#3A2A00` |
| 배경 틴트 | `--accent-bg` | `#FAF3DE` |
| 짙은 골드 텍스트 | `--accent-ink` | `#6B4F12` |

용도: 알림 배지, 2차 액션(`.btn-accent`), 즐겨찾기 별, 진행 단계 현재, 도구 요청(리소스) 표식.

### 기능색
| 의미 | 토큰 | HEX | 사용처 |
|------|------|-----|--------|
| 성공/보유/승인 | `--green` / `--green-bg` | `#0E8A66` / `#E3F4EE` | 지식(RAG), 도구 “보유”, 사용중 |
| 경고/반려 | `--red` / `--red-bg` | `#D2403A` / `#FBECEA` | 반려, 권한 필요 도구 |

### 중립색
`--ink #15223B`(본문) · `--gray #565F6D` · `--gray-lt #8A94A3` · `--line #E5E9F1` · `--line-strong #D3D9E4` · `--canvas #F4F7FB`(페이지 배경) · `--card #FFFFFF`.
페이지 배경엔 우상단 신한 블루 저채도 글로우(`radial-gradient`)로 은은한 입체감.

---

## 2. 타이포그래피
- 서체: **Pretendard**(폴백 `Malgun Gothic, system-ui`). — 신한 전용서체 대체용 고딕.
- 기준 크기 14px / line-height 1.6 / `letter-spacing: -.01em`.
- 위계: 페이지 타이틀 25px·800 · 섹션 타이틀 14.5px·800 · 본문 13–14px · 캡션 11–12px.

## 3. 형태·간격
- 라운드: `--r-lg 18` · `--r-md 14` · `--r-sm 10` · 버튼 11 · 알약(pill) `999px`.
- 그림자: `--shadow`(카드, 저스프레드 2단) · `--shadow-hover`(부상/모달, 레이어드).
- 포커스: `--ring`(신한 블루 3px 소프트 링) — 입력/버튼 공통.
- 카드 패딩 16–18px, 섹션 간격 26–28px, 부드러운 이징(`.12–.16s ease`).

## 4. 컴포넌트 규칙(요약)
- **버튼**: 1차=신한 블루 채움, 2차=골드 채움, ghost=테두리, green/danger=기능색.
- **탭**: 언더라인형(활성 2px 신한 블루), 배경 박스 없음.
- **필터/셀렉트**: 알약형, 커스텀 셰브론(오른쪽 12px).
- **상태 pill**: 사용중=블루, 보유/승인=그린, 요청중=골드, 반려/권한필요=레드.
- **아이콘 사각(sq)**: navy(블루 틴트)/green(그린 틴트)/amber(골드 틴트).
- **플로팅 액션(FAB)**: 신한 블루 그라데이션, 드래그 이동 가능.

## 5. CI → 앱 매핑 원칙
1. 신한 블루를 **주 액션·아이덴티티** 전반에 적용(기존 네이비/퍼플 대체).
2. 골드는 **보조 강조**로 한정(남용 금지) — 배지·별·요청 표식 등.
3. 그린/레드는 브랜드색이 아닌 **기능 신호**로만 사용(성공/경고).
4. 넓은 면적은 저채도 틴트(`*-soft`, `*-bg`)로, 포인트만 원색.

> 변경 지점: 팔레트는 `src/styles.css :root` 한 곳에서 관리. 색을 조정하려면 토큰만 바꾸면 전 화면에 반영됩니다.
